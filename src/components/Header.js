import React from "react";
import Button from "./Button";

const Header = () => {
  return (
    <div className="header">
      <h2>To-Do List</h2>
      <Button />
    </div>
  );
};

export default Header;
